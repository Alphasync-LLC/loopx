"""Repository-wide semantic vocabulary registry and inventory.

``vocabulary_v0.json`` is the curated registry (what each cross-module vocabulary
may carry, who defines it, how vocabularies relate). ``inventory_v0.json`` is the
generated map of every closed-set vocabulary carrier under ``loopx/``. Both are
checked, never imported, by product code; see the semantic vocabulary RFC.
"""
